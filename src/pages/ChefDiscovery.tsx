import { useState, useEffect } from 'react';import { useNavigate } from 'react-router-dom';import { Layout } from '@/components/layout/Layout';import { Card, CardContent } from '@/components/ui/card';import { Button } from '@/components/ui/button';import { Badge } from '@/components/ui/badge';import { Input } from '@/components/ui/input';import {   ChefHat, Star, MapPin, Clock, Search, Filter,   Sparkles, Award, Utensils, ArrowUpDown, X} from 'lucide-react';import * as api from '@/services/api';import type { Dish } from '@/types';type ChefWithData = ReturnType<typeof api.getApprovedChefsWithRatings>['data'] extends (infer T)[] | undefined ? T : never;type SortOption = 'rating' | 'experience' | 'orders';type FilterOption = 'all' | 'customizable' | 'veg' | 'non-veg';export const ChefDiscovery = () => {  const navigate = useNavigate();  const [chefs, setChefs] = useState<ChefWithData[]>([]);  const [loading, setLoading] = useState(true);  const [searchQuery, setSearchQuery] = useState('');  const [sortBy, setSortBy] = useState<SortOption>('rating');  const [filterBy, setFilterBy] = useState<FilterOption>('all');  const [showFilters, setShowFilters] = useState(false);  useEffect(() => {    loadChefs();  }, []);  const loadChefs = () => {    setLoading(true);    const response = api.getApprovedChefsWithRatings();    if (response.success && response.data) {      setChefs(response.data);    }    setLoading(false);  };  const getFilteredAndSortedChefs = () => {    let filtered = [...chefs];    // Search filter    if (searchQuery) {      const query = searchQuery.toLowerCase();      filtered = filtered.filter(chef =>         chef.name.toLowerCase().includes(query) ||        chef.specialty?.toLowerCase().includes(query) ||        chef.serviceArea?.toLowerCase().includes(query)      );    }    // Category filter    if (filterBy === 'customizable') {      filtered = filtered.filter(chef => chef.dishes.some(d => d.allowsCustomization));    } else if (filterBy === 'veg') {      filtered = filtered.filter(chef => chef.dishes.some(d => d.category === 'veg'));    } else if (filterBy === 'non-veg') {      filtered = filtered.filter(chef => chef.dishes.some(d => d.category === 'non-veg'));    }    // Sort    switch (sortBy) {      case 'rating':        filtered.sort((a, b) => (b.avgRating || 0) - (a.avgRating || 0));        break;      case 'experience':        filtered.sort((a, b) => (b.totalOrders || 0) - (a.totalOrders || 0));        break;      case 'orders':        filtered.sort((a, b) => (b.totalOrders || 0) - (a.totalOrders || 0));        break;    }    return filtered;  };  const getSpecialDish = (dishes: Dish[]) => {
    return dishes.find(d => d.isSpecial) || dishes[0];
  };

  const getMenuPreview = (chef: ChefWithData) => {
    const chart = chef.menuCharts?.[0];
    if (!chart) return [];
    const today = new Date().toISOString().split('T')[0];
    const upcoming = chart.days.filter(day => day.date >= today).slice(0, 3);
    return upcoming.map(day => {
      const slots = day.slots || {};
      const mealIds = [slots.lunch?.mealId, slots.dinner?.mealId, slots.breakfast?.mealId].filter(Boolean) as string[];
      const mealNames = mealIds.map(id => chef.dishes.find(d => d.id === id)?.name || id);
      return { date: day.date, meals: mealNames.slice(0, 2) };
    });
  };
  const planOptions = [    { id: 'basic', name: 'Basic', slots: 'Lunch', price: '₹2,999' },    { id: 'standard', name: 'Standard', slots: 'Lunch + Dinner', price: '₹4,499' },    { id: 'premium', name: 'Premium', slots: 'Breakfast + Lunch + Dinner', price: '₹5,999' },  ];  const filteredChefs = getFilteredAndSortedChefs();  return (    <Layout>      <div className="container py-8 px-4">        <div className="max-w-5xl mx-auto">          {/* Header */}          <div className="text-center mb-8">            <h1 className="font-display text-3xl md:text-4xl font-bold mb-3">              Discover Your Perfect Chef            </h1>            <p className="text-muted-foreground text-lg max-w-2xl mx-auto">              Browse our curated selection of talented home chefs. Each brings unique flavors and specialties to your table.            </p>          </div>          {/* Search & Filter Bar */}          <div className="mb-8 space-y-4">            <div className="flex gap-3">              <div className="relative flex-1">                <Search className="absolute left-3 top-1/2 -translate-y-1/2 w-5 h-5 text-muted-foreground" />                <Input                  placeholder="Search by name, cuisine, or area..."                  value={searchQuery}                  onChange={(e) => setSearchQuery(e.target.value)}                  className="pl-10 h-12"                />                {searchQuery && (                  <button                     onClick={() => setSearchQuery('')}                    className="absolute right-3 top-1/2 -translate-y-1/2"                  >                    <X className="w-4 h-4 text-muted-foreground hover:text-foreground" />                  </button>                )}                        </div>              <Button                 variant="outline"                 size="lg"                onClick={() => setShowFilters(!showFilters)}                className={showFilters ? 'bg-primary/10' : ''}              >                <Filter className="w-4 h-4 mr-2" />                Filters              </Button>            </div>            {/* Filter Options */}            {showFilters && (              <div className="p-4 rounded-xl bg-secondary/50 border animate-slide-up">                <div className="flex flex-wrap gap-4">                  <div className="space-y-2">                    <p className="text-sm font-medium text-muted-foreground flex items-center gap-1">                      <ArrowUpDown className="w-3 h-3" /> Sort by                    </p>                    <div className="flex gap-2">                      {[                        { value: 'rating', label: 'Highest Rated' },                        { value: 'experience', label: 'Most Experienced' },                      ].map((option) => (                        <Button                          key={option.value}                          variant={sortBy === option.value ? 'default' : 'outline'}                          size="sm"                          onClick={() => setSortBy(option.value as SortOption)}                        >                          {option.label}                        </Button>                      ))}                        </div>                  </div>                  <div className="space-y-2">                    <p className="text-sm font-medium text-muted-foreground flex items-center gap-1">                      <Filter className="w-3 h-3" /> Filter by                    </p>                    <div className="flex gap-2 flex-wrap">                      {[                        { value: 'all', label: 'All' },                        { value: 'customizable', label: 'Customizable' },                        { value: 'veg', label: 'Veg Options' },                        { value: 'non-veg', label: 'Non-Veg Options' },                      ].map((option) => (                        <Button                          key={option.value}                          variant={filterBy === option.value ? 'default' : 'outline'}                          size="sm"                          onClick={() => setFilterBy(option.value as FilterOption)}                        >                          {option.label}                        </Button>                      ))}                        </div>                  </div>                </div>              </div>            )}                        </div>          {/* Results Count */}          <p className="text-sm text-muted-foreground mb-4">            {filteredChefs.length} chef{filteredChefs.length !== 1 ? 's' : ''} available          </p>          {/* Chefs Grid */}          {loading ? (            <div className="grid md:grid-cols-2 gap-6">              {[1, 2, 3, 4].map((i) => (                <Card key={i} className="shadow-card animate-pulse">                  <CardContent className="p-6">                    <div className="flex gap-4">                      <div className="w-20 h-20 rounded-xl bg-muted" />                      <div className="flex-1 space-y-3">                        <div className="h-5 bg-muted rounded w-2/3" />                        <div className="h-4 bg-muted rounded w-1/2" />                        <div className="h-4 bg-muted rounded w-3/4" />                      </div>                    </div>                  </CardContent>                </Card>              ))}                        </div>          ) : filteredChefs.length === 0 ? (            <div className="text-center py-16">              <ChefHat className="w-16 h-16 mx-auto text-muted-foreground/50 mb-4" />              <h3 className="font-display text-xl font-bold mb-2">No chefs found</h3>              <p className="text-muted-foreground">Try adjusting your search or filters</p>            </div>          ) : (            <div className="grid md:grid-cols-2 gap-6">              {filteredChefs.map((chef) => {
                const specialDish = getSpecialDish(chef.dishes);
                const hasCustomizable = chef.dishes.some(d => d.allowsCustomization);
                const menuPreview = getMenuPreview(chef);
                
                return (
                  <Card                     key={chef.id}                     className="shadow-card hover:shadow-card-hover transition-all cursor-pointer group overflow-hidden"                    onClick={() => navigate(`/chef/${chef.id}`)}                  >                    <CardContent className="p-0">                      {/* Chef Info Section */}                      <div className="p-6">                        <div className="flex gap-4">                          {/* Profile Image */}                          <div className="relative">                            <div className="w-20 h-20 rounded-xl bg-chef flex items-center justify-center group-hover:scale-105 transition-transform">                              <ChefHat className="w-10 h-10 text-chef-foreground" />                            </div>                            {(chef.avgRating || 0) >= 4.5 && (                              <div className="absolute -top-2 -right-2 w-6 h-6 rounded-full bg-warning flex items-center justify-center">                                <Award className="w-3 h-3 text-warning-foreground" />                              </div>                            )}                        </div>                          {/* Details */}                          <div className="flex-1 min-w-0">                            <h3 className="font-display font-bold text-lg truncate group-hover:text-primary transition-colors">                              {chef.name}                            </h3>                            <p className="text-sm text-muted-foreground mb-2">{chef.specialty}</p>                                                        {/* Rating */}                            <div className="flex items-center gap-2 mb-2">                              <div className="flex items-center gap-1 px-2 py-0.5 rounded-full bg-warning/10">                                <Star className="w-3 h-3 fill-warning text-warning" />                                <span className="text-sm font-medium">{chef.avgRating?.toFixed(1) || 'New'}</span>                              </div>                              <span className="text-xs text-muted-foreground">                                ({chef.reviewCount || 0} reviews)                              </span>                              {chef.totalOrders && chef.totalOrders > 500 && (                                <Badge variant="secondary" className="text-xs">                                  {chef.totalOrders}+ orders                                </Badge>                              )}                        </div>                            {/* Location */}                            <div className="flex items-center gap-1 text-xs text-muted-foreground">                              <MapPin className="w-3 h-3" />                              <span className="truncate">{chef.serviceArea}</span>                            </div>                          </div>                        </div>                        {/* Badges */}                        <div className="flex flex-wrap gap-2 mt-4">                          {hasCustomizable && (                            <Badge variant="outline" className="text-xs">                              <Sparkles className="w-3 h-3 mr-1" />                              Customizable                            </Badge>                          )}                          <Badge variant="outline" className="text-xs">                            <Utensils className="w-3 h-3 mr-1" />                            {chef.dishes.length} dishes                          </Badge>                          {chef.deliverySlots?.includes('lunch') && chef.deliverySlots?.includes('dinner') && (                            <Badge variant="outline" className="text-xs">                              <Clock className="w-3 h-3 mr-1" />                              Lunch & Dinner                            </Badge>                          )}                        </div>                      </div>                      <div className="px-6 pb-4">                        <div className="rounded-lg border bg-secondary/40 p-3">                          <p className="text-xs font-medium text-muted-foreground mb-2">Subscription plans</p>                          <div className="grid grid-cols-3 gap-2 text-xs">                            {planOptions.map((plan) => (                              <div key={plan.id} className="rounded-md border bg-background px-2 py-1">                                <p className="font-medium">{plan.name}</p>                                <p className="text-[11px] text-muted-foreground">{plan.slots}</p>                                <p className="text-xs font-semibold text-primary">{plan.price}</p>                              </div>                            ))}                          </div>                        </div>                      </div>                      {/* Special Dish Highlight */}                      {specialDish && (                        <div className="px-6 pb-6">                          <div className="p-3 rounded-lg bg-primary/5 border border-primary/10">                            <p className="text-xs font-medium text-primary mb-1 flex items-center gap-1">                              <Sparkles className="w-3 h-3" />                              {specialDish.isSpecial ? "Chef's Special" : "Popular Dish"}                            </p>                            <p className="font-medium text-sm">{specialDish.name}</p>                            <p className="text-xs text-muted-foreground mt-0.5">                              {specialDish.nutritionalInfo.calories} cal • {specialDish.nutritionalInfo.protein}g protein                            </p>                          </div>                          {menuPreview.length > 0 && (                            <div className="mt-3 p-3 rounded-lg bg-secondary/50 border border-border/50">                              <p className="text-xs font-medium text-muted-foreground mb-2">Weekly menu preview</p>                              <div className="space-y-1">                                {menuPreview.map((item) => (                                  <div key={item.date} className="text-xs text-muted-foreground flex items-center justify-between">                                    <span>{item.date}</span>                                    <span className="text-foreground">{item.meals.join(' • ')}</span>                                  </div>                                ))}                        </div>                            </div>                          )}                        </div>                      )}                    </CardContent>                  </Card>                );              })}                        </div>          )}                        </div>      </div>    </Layout>  );};export default ChefDiscovery;




